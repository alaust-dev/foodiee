version: '3.8'

services:
  webserver:
    build: .
    ports:
      - "8080:8080"

#  swagger-ui:
#    image: swaggerapi/swagger-ui
#    ports:
#      - "80:8080"
#    volumes:
#      - ./api.yaml:/api.yaml
#    environment:
#      SWAGGER_JSON: /api.yaml
